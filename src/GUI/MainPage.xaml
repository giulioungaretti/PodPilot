<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GUI"
    xmlns:controls="using:GUI.Controls"
    xmlns:models="using:DeviceCommunication.Models"
    xmlns:viewModels="using:GUI.ViewModels"
    xmlns:converters="using:GUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid x:Name="PageRoot">
        <Grid.Background>
            <AcrylicBrush TintColor="{ThemeResource SolidBackgroundFillColorBase}"
                          TintOpacity="0.5"
                          FallbackColor="{ThemeResource SolidBackgroundFillColorBase}" />
        </Grid.Background>

        <!-- Content with devices -->
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      Visibility="{x:Bind ViewModel.HasDiscoveredDevices, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Padding="40,20,40,40" Spacing="32" MaxWidth="1200">
                
                <!-- Header -->
                <StackPanel Spacing="8">
                    <TextBlock Text="PodPilot" 
                               FontSize="32" 
                               FontWeight="Bold" />
                    <TextBlock Text="Monitor your AirPods battery and status" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               FontSize="14" />
                </StackPanel>

                <!-- Discovered Devices Section -->
                <StackPanel Spacing="16">
                    <Grid>
                        <TextBlock Text="📡 Nearby Devices" 
                                   FontSize="20" 
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"
                                   FontFamily="Segoe UI Emoji" />
                        
                        <StackPanel Orientation="Horizontal" 
                                    Spacing="12" 
                                    HorizontalAlignment="Right"
                                    Visibility="{x:Bind ViewModel.IsScanning, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <controls:PulsatingDot IsActive="True" Size="20" />
                        </StackPanel>
                    </Grid>

                    <!-- Discovered Devices List -->
                    <ItemsControl ItemsSource="{x:Bind ViewModel.DiscoveredDevices, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="viewModels:AirPodsDeviceViewModel">
                                <controls:AirPodsCard Device="{x:Bind Mode=OneWay}" 
                                                      Margin="0,0,0,16"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </StackPanel>

                <!-- Info Footer -->
                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                        CornerRadius="8"
                        Padding="16"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontFamily="Segoe UI Emoji">
                            <Run Text="💡" />
                            <Run Text="Tip: Your AirPods don't need to be paired to view their status." />
                        </TextBlock>
                        <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="They just need to be in range and broadcasting." />
                        </TextBlock>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Empty state centered -->
        <Grid Visibility="{x:Bind ViewModel.HasDiscoveredDevices, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" 
                        HorizontalAlignment="Center"
                        Spacing="40"
                        MaxWidth="600">
                
                <!-- Header (simplified for empty state) -->
                <StackPanel Spacing="8" HorizontalAlignment="Center">
                    <TextBlock Text="PodPilot" 
                               FontSize="32" 
                               FontWeight="Bold"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="Monitor your AirPods battery and status" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               FontSize="14"
                               HorizontalAlignment="Center" />
                </StackPanel>

                <!-- Empty State -->
                <controls:EmptyState Icon="🎧" 
                                     Title="No devices found"
                                     Message="Open your AirPods case nearby to discover devices"/>

                <!-- Info Footer -->
                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                        CornerRadius="8"
                        Padding="16"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontFamily="Segoe UI Emoji">
                            <Run Text="💡" />
                            <Run Text="Tip: Your AirPods don't need to be paired to view their status." />
                        </TextBlock>
                        <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="They just need to be in range and broadcasting." />
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
