<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GUI.Controls.PulsatingDot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Viewbox Width="{x:Bind Size, Mode=OneWay}" 
             Height="{x:Bind Size, Mode=OneWay}">
        <Grid Width="24" Height="24">
            <!-- Outer pulsating ring -->
            <Ellipse x:Name="OuterRing"
                     Width="24" 
                     Height="24"
                     Fill="{x:Bind DotBrush, Mode=OneWay}"
                     Opacity="0.3">
                <Ellipse.RenderTransform>
                    <ScaleTransform x:Name="OuterRingScale" 
                                    ScaleX="1" 
                                    ScaleY="1"
                                    CenterX="12"
                                    CenterY="12"/>
                </Ellipse.RenderTransform>
            </Ellipse>

            <!-- Middle pulsating ring -->
            <Ellipse x:Name="MiddleRing"
                     Width="18" 
                     Height="18"
                     Fill="{x:Bind DotBrush, Mode=OneWay}"
                     Opacity="0.5"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center">
                <Ellipse.RenderTransform>
                    <ScaleTransform x:Name="MiddleRingScale" 
                                    ScaleX="1" 
                                    ScaleY="1"
                                    CenterX="9"
                                    CenterY="9"/>
                </Ellipse.RenderTransform>
            </Ellipse>

            <!-- Core dot -->
            <Ellipse x:Name="CoreDot"
                     Width="12" 
                     Height="12"
                     Fill="{x:Bind DotBrush, Mode=OneWay}"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center">
                <Ellipse.RenderTransform>
                    <ScaleTransform x:Name="CoreDotScale" 
                                    ScaleX="1" 
                                    ScaleY="1"
                                    CenterX="6"
                                    CenterY="6"/>
                </Ellipse.RenderTransform>
            </Ellipse>
        </Grid>
    </Viewbox>
</UserControl>
